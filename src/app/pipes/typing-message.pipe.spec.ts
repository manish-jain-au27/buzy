import { TypingMessagePipe } from './typing-message.pipe';

describe('TypingMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new TypingMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
